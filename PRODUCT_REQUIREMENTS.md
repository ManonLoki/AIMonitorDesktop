# AIMonitorDesktop 产品要求

当前产品基线：`2.0.2`（多原生窗口与桌宠模式架构）。

## 目标

基于 `/Users/manonloki/Documents/my-work/ai/AiMonitorAndroid` 已实现的业务逻辑、HTTP API 与界面文案，1:1 复刻可运行于 Windows 和 macOS 的桌面版。

## 强制要求

1. 界面保持简洁，不引入与监控画布无关的仪表盘或装饰信息。
2. 首次启动且没有历史窗口状态时，主界面立即最大化；后续启动直接恢复已持久化
   的主界面或桌宠模式，不允许先显示错误窗口再切换。

## 功能基线

- 监控画布支持 1–5 行、1–5 列，共 25 个可寻址宫格。
- 宫格显示 AI 名称、用户名、图片、内容与更新时间。
- 图片支持 JPG、PNG、GIF，以及“等比缩放”和“填满裁剪”两种显示模式。
- 提供与 Android 版兼容的 `/health`、`/api/device`、`/api/config`、`/api/images`、`/api/slots/{slot}` HTTP API。
- HTTP 端口从 10241 起自动选择，支持 `_aimonitor._tcp.` mDNS/DNS-SD 和 UDP 8080 广播发现。
- 设置持久化：设备名称、宫格行列、图片显示模式、开机自启与窗口位置/大小。
- 页面文案以 Android 版现有中文文案为准。
- 主界面与桌宠模式互斥显示，并共享同一套监控状态与后台服务。
- 桌宠支持 1×1、1×2、2×1、1×3、3×1、2×2 布局，适配 1～4 个 AI 展示，并支持循环分页、拖动、连续等比缩放、始终置顶和位置锁定。
- 右键桌宠在其当前所在显示器打开独立设置窗口；双击非控制区域返回主界面，
  隐藏到托盘不改变模式。
- 桌宠尺寸、布局、关注槽位和窗口几何持久化，并在 DPI、分辨率或显示器变化后
  保持窗口完整可见、可操作。
- 桌宠尺寸表示单格边长，统一最小 32px；单格上限为屏幕逻辑最短边除以 4、再除以布局最长轴格子数。
- 桌宠在 macOS 同一显示器的不同桌面空间中保持可见，分页栏仅在鼠标位于窗口内时显示。
- macOS 使用菜单栏托盘作为常驻入口，不显示 Dock 图标。
- 托盘菜单按当前模式只显示有效操作：看板模式为“桌宠模式、显示看板、开机
  自启、退出”，桌宠模式为“看板模式、锁定桌宠、开机自启、退出”。

桌宠行为的规范性细节与验收标准以
[`docs/DESKTOP_PET_MODE_DESIGN.md`](./docs/DESKTOP_PET_MODE_DESIGN.md) 为准。
