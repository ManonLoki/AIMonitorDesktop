{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main, pet, and pet settings windows",
  "windows": ["main", "pet", "pet-settings"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
